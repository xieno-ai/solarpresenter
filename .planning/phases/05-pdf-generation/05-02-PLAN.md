---
phase: 05-pdf-generation
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified: []
autonomous: false
requirements:
  - PROP-08

must_haves:
  truths:
    - "User can click Download PDF in the FAB and a file download begins within 15 seconds"
    - "The downloaded PDF has exactly 11 pages (no blank trailing page)"
    - "Page backgrounds, colors, and gradients appear in the PDF (not stripped to white)"
    - "Custom fonts (Bebas Neue, DM Sans, Montserrat) are visible in the PDF"
    - "Charts on pages 3/5 (Recharts) are visible in the PDF"
    - "PDF file size is under 5 MB"
    - "Page breaks fall cleanly between proposal sections"
  artifacts:
    - path: "src/app/proposal/pdf/page.tsx"
      provides: "PDF render target that Playwright navigates to"
    - path: "src/app/api/pdf/route.ts"
      provides: "Returns binary PDF from Playwright"
  key_links:
    - from: "ProposalFAB Download PDF button"
      to: "/api/pdf?d=..."
      via: "fetch call in handleDownloadPdf"
      pattern: "fetch.*api/pdf"
---

<objective>
Human verification that the PDF export works correctly: correct page count, visual fidelity (colors, fonts, charts), file size under 5 MB, and clean page breaks.

Purpose: Gate Phase 5 completion. The PDF must match the web preview — this is the core deliverable PROP-08.
Output: Human sign-off that the PDF is production-ready, or a list of issues to fix.
</objective>

<execution_context>
@C:/Users/David/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/David/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:/Cursor/Solar Presenter/.planning/phases/05-pdf-generation/05-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete PDF export pipeline:
    - `/proposal/pdf` route: clean render target for Playwright (all 11 pages, no FAB, no scroll-snap)
    - `/api/pdf` GET route: Playwright launches Chromium, navigates to /proposal/pdf, returns PDF buffer
    - `ProposalFAB`: Download PDF action (4th speed-dial item, spinner during generation, error on failure)
    - File named: `NorthernNRG_[LastName]_[YYYY-MM-DD].pdf`
  </what-built>
  <how-to-verify>
    **Prerequisites:**
    1. Ensure `.env.local` contains: `NEXT_PUBLIC_BASE_URL=http://localhost:3001`
    2. Start the dev server: `npm run dev` (runs on port 3001)
    3. Navigate to `/enter` and generate a test proposal (use any test data — Paul Friesen fixture values work)

    **Step 1 — Trigger download:**
    - On the proposal page, click the green FAB button (bottom-right)
    - Click "Download PDF" in the speed dial
    - Verify the FAB item shows a spinner + "Generating..." immediately
    - Verify the Download PDF button is disabled while generating (no double-click)
    - After 5-15 seconds, a file download begins

    **Step 2 — Check file:**
    - File name follows pattern: `NorthernNRG_[LastName]_[YYYY-MM-DD].pdf`
    - File size is under 5 MB (check in file browser)

    **Step 3 — Open PDF and verify visually:**
    - PDF has exactly 11 pages (not 10, not 12)
    - Page 1: Cover page shows correct customer name, system size, green/dark layout — NOT a white background
    - Page 2: Table of Contents with NRG styling and colors
    - Page 3: Net Metering table with green surplus rows, rate cards
    - Page 4: Carbon Credits page with dark green left panel and mint-colored numbers
    - Page 5: All-In Costs with blue/green column panels side by side
    - Pages 6-11: Static marketing pages — colors and branding visible
    - Check that fonts look correct (not Arial/system fallback — Bebas Neue for headings, DM Sans for body)
    - Check that charts on Pages 3/5 are visible (if Recharts didn't render, pages show empty chart areas)

    **Step 4 — Check error handling:**
    - Optional: temporarily rename `.env.local` to break the URL, try downloading — should see an error message near the FAB within 35 seconds (not silent failure)
    - Restore `.env.local` afterward

    **Known acceptable issues (do NOT flag these):**
    - Minor layout differences vs web preview due to Letter paper vs screen viewport (slight padding differences)
    - Chart sizing may differ slightly from web due to viewport normalization
  </how-to-verify>
  <resume-signal>
    Type "approved" if the PDF looks correct, OR describe specific issues (e.g., "Page 1 background is white", "Fonts look wrong on page 3", "PDF has 12 pages") so they can be fixed.
  </resume-signal>
</task>

</tasks>

<verification>
Human-verified: PDF downloads successfully with correct branding, 11 pages, colors, fonts, and charts visible.
</verification>

<success_criteria>
- PDF downloads within 15 seconds of clicking Download PDF
- PDF has exactly 11 pages
- All page backgrounds and colors visible (not stripped to white)
- Custom fonts visible (not system fallback)
- File size under 5 MB
- File named `NorthernNRG_[LastName]_[YYYY-MM-DD].pdf`
- No silent failure — error shown if generation fails
</success_criteria>

<output>
After human approval, create `.planning/phases/05-pdf-generation/05-02-SUMMARY.md`

Mark Phase 5 complete in STATE.md and ROADMAP.md.
</output>

---
phase: 04-proposal-rendering
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/app/proposal/pages/TableOfContentsPage.tsx
  - src/app/proposal/pages/PriceHistoryPage.tsx
  - src/app/proposal/pages/WhatsComingPage.tsx
  - src/app/proposal/pages/WhyUsPage.tsx
  - src/app/proposal/pages/WarrantyPage.tsx
  - src/app/proposal/pages/FAQPage.tsx
  - src/app/proposal/pages/NextStepsPage.tsx
  - src/app/proposal/page.tsx
autonomous: true
requirements:
  - PROP-03
  - PROP-07
  - PROP-10

must_haves:
  truths:
    - "Page 2 (Table of Contents) shows 9 numbered sections matching the PDF layout with red number badges and section titles"
    - "Pages 6-11 (static marketing pages) all render visually complete content — not blank or placeholder"
    - "Page 6 (Price History) includes a static inline SVG chart representing historical electricity rate increases"
    - "All static pages use NRG branding — green/dark green/gold color scheme, Bebas Neue section labels"
    - "The full proposal scrolls through all 11 pages in sequence using CSS scroll-snap"
  artifacts:
    - path: "src/app/proposal/pages/TableOfContentsPage.tsx"
      provides: "Page 2 — static TOC with 9 numbered sections"
      exports: ["TableOfContentsPage"]
    - path: "src/app/proposal/pages/PriceHistoryPage.tsx"
      provides: "Page 6 — static electricity price history with inline SVG chart"
      exports: ["PriceHistoryPage"]
    - path: "src/app/proposal/pages/WhatsComingPage.tsx"
      provides: "Page 7 — two-card layout: solar forces"
      exports: ["WhatsComingPage"]
    - path: "src/app/proposal/pages/WhyUsPage.tsx"
      provides: "Page 8 — NRG differentiators and stats"
      exports: ["WhyUsPage"]
    - path: "src/app/proposal/pages/WarrantyPage.tsx"
      provides: "Page 9 — 4 warranty cards"
      exports: ["WarrantyPage"]
    - path: "src/app/proposal/pages/FAQPage.tsx"
      provides: "Page 10 — FAQ accordion content (static)"
      exports: ["FAQPage"]
    - path: "src/app/proposal/pages/NextStepsPage.tsx"
      provides: "Page 11 — 6-step installation timeline"
      exports: ["NextStepsPage"]
  key_links:
    - from: "src/app/proposal/page.tsx"
      to: "all 7 static page components"
      via: "direct import and render in positions 2, 6-11"
      pattern: "TableOfContentsPage|PriceHistoryPage|WhatsComingPage|WhyUsPage|WarrantyPage|FAQPage|NextStepsPage"
---

<objective>
Build the Table of Contents (Page 2) and all six static marketing pages (Pages 6-11) as self-contained React components with hardcoded content matching the Northern NRG PDF template. Update page.tsx to render all 11 pages in the scroll-snap container.

Purpose: Completing these pages closes out the full 11-page proposal rendering and satisfies PROP-03 and PROP-07. These run in parallel with Plan 02 (dynamic pages) since they touch different files.
Output: A complete 11-page scrollable proposal visible in-browser.
</objective>

<execution_context>
@C:/Users/David/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/David/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-proposal-rendering/04-CONTEXT.md
@.planning/phases/04-proposal-rendering/04-RESEARCH.md
@.planning/phases/04-proposal-rendering/04-01-SUMMARY.md

<interfaces>
NRG color variables (from proposal.css — all scoped to .proposal-root):
- `--nrg-green: #00793f`
- `--nrg-dark-green: #17573a`
- `--nrg-gold: #f5a623`
- `--nrg-page-bg: #ecf7ff`
- `--nrg-card-bg: #ffffff`
- `--nrg-light-green-bg: #e7faf0`
- `--nrg-pale-green-bg: #f5fff7`
- `--nrg-gold-banner-inner: #fff7eb`
- `--nrg-red-badge: #df584d`
- `--nrg-text-primary: #1a2b3c`
- `--nrg-text-secondary: #6b8299`
- `--nrg-text-heading: #0d1b2a`

Font variables:
- `--font-display` (Bebas Neue) — section sub-labels, uppercase section labels
- `--font-montserrat` (Montserrat) — callout text, step numbers
- `--font-sans` (DM Sans) — all body text and headings
- `--font-mono` (JetBrains Mono) — rarely needed in static pages

Page structure pattern (all pages follow this shell):
```tsx
export function XxxPage() {
  return (
    <div className="proposal-page" style={{ background: 'var(--nrg-page-bg)' }}>
      {/* page content */}
    </div>
  );
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TableOfContentsPage (Page 2) + PriceHistoryPage (Page 6)</name>
  <files>
    src/app/proposal/pages/TableOfContentsPage.tsx
    src/app/proposal/pages/PriceHistoryPage.tsx
    src/app/proposal/page.tsx
  </files>
  <action>
**Create src/app/proposal/pages/TableOfContentsPage.tsx**

Static component. No props. Content matching PDF Page 2.

Layout:
- Page background: `var(--nrg-page-bg)` (`#ecf7ff`)
- Left accent strip: narrow vertical bar in `var(--nrg-green)`
- Main heading: "WHAT'S INSIDE" or "YOUR PROPOSAL" in DM Sans Bold (`var(--nrg-text-heading)`)
- Bebas Neue sub-label: "TABLE OF CONTENTS"
- 9 section items arranged in a two-column grid. Each item:
  - Large section number badge: circular, `var(--nrg-red-badge)` (`#df584d`) background, white number in DM Sans Bold (~2rem)
  - Section title: DM Sans Bold in `var(--nrg-text-heading)`
  - Short description line: DM Sans Regular in `var(--nrg-text-secondary)`

The 9 sections (hardcoded, match the PDF):
1. Net Metering — "Month-by-month solar production vs. consumption analysis"
2. Carbon Credits — "Annual CO₂ offset and Alberta carbon credit estimates"
3. True All-In Costs — "Cash purchase and finance option cost breakdown"
4. Electricity Price History — "30-year trend showing rate increases"
5. What's Coming — "Forces driving electricity prices higher"
6. Why Northern NRG — "Our commitment to quality and service"
7. Warranty & Protection — "Comprehensive coverage for your investment"
8. Frequently Asked Questions — "Common questions from solar customers"
9. Your Next Steps — "How we get your solar system installed"

Northern NRG footer bar at bottom: `var(--nrg-green)` background, white "NORTHERN NRG" text centered.

**Create src/app/proposal/pages/PriceHistoryPage.tsx**

Static component. No props. Content matching PDF Page 6.

Layout:
- Page background: `var(--nrg-page-bg)` (`#ecf7ff`)
- Heading: "ELECTRICITY PRICE HISTORY" (DM Sans Bold) with Bebas Neue sub-label
- Static inline SVG chart showing historical Alberta electricity rate trend (~15-20 years of rising rates). Use approximate data points representing a rising trend with year labels on X-axis and ¢/kWh on Y-axis. The chart area background: `var(--nrg-pale-green-bg)`. Use red gradient fills (#ed6b52, #fb8365) matching the PDF chart area fills (filled area chart, not just a line). SVG should be 100% width, ~300px height, with hardcoded path data representing a clearly upward trend.
- Stats overlay: 2-3 callout boxes showing notable rate increases (e.g. "↑ 47% since 2010", "5% avg annual increase")
- Source note: "Source: Alberta Utilities Commission historical rate data"

The SVG for the price history chart should be a simple area chart: draw a path for the rising rate curve from left to right with ~15 data points, fill the area below in a red gradient. Use `<defs><linearGradient>` for the gradient fill. Approximate data: rates rising from ~10¢/kWh in 2010 to ~16.8¢/kWh in 2024.

**Update src/app/proposal/page.tsx:**
Import TableOfContentsPage and PriceHistoryPage. Render in positions 2 and 6. At this point page.tsx renders: [Cover, TOC, NetMetering, CarbonCredits, AllInCosts, PriceHistory, placeholder×5].
  </action>
  <verify>
    <automated>cd "D:/Cursor/Solar Presenter" && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>TableOfContentsPage and PriceHistoryPage created and imported in page.tsx. Both render with NRG branding and correct content. TypeScript compiles with no errors.</done>
</task>

<task type="auto">
  <name>Task 2: Static pages 7-11 (WhatsComingPage through NextStepsPage) + full 11-page assembly</name>
  <files>
    src/app/proposal/pages/WhatsComingPage.tsx
    src/app/proposal/pages/WhyUsPage.tsx
    src/app/proposal/pages/WarrantyPage.tsx
    src/app/proposal/pages/FAQPage.tsx
    src/app/proposal/pages/NextStepsPage.tsx
    src/app/proposal/page.tsx
  </files>
  <action>
Create all five remaining static pages. Each is a self-contained component with no props. Content is hardcoded to match the Northern NRG PDF template. All pages use the `.proposal-page` class and NRG CSS variables.

**Create src/app/proposal/pages/WhatsComingPage.tsx (Page 7)**
Layout: Two large side-by-side cards on `var(--nrg-page-bg)` background.
- Left card background: `var(--nrg-gold-banner-inner)` (`#fff7eb`) — "RISING DEMAND" or "GRID PRESSURE" force
- Right card background: a contrasting light tone (e.g. `#efecf0`) — "AGING INFRASTRUCTURE" or "RATE INCREASES" force
- Each card: icon area (use a simple SVG icon — lightning bolt for demand, house/grid icon for infrastructure), bold heading in DM Sans Bold, body paragraph explaining the solar case
- Bebas Neue sub-label at top: "WHAT'S COMING"
- Page heading: "WHY ELECTRICITY PRICES WILL KEEP RISING" (DM Sans Bold, `var(--nrg-text-heading)`)
Content (hardcoded): Rising demand from EVs and electrification. Aging grid infrastructure requiring capital investment. Carbon pricing increasing fossil fuel costs. All driving electricity rate increases for the foreseeable future.

**Create src/app/proposal/pages/WhyUsPage.tsx (Page 8)**
Layout: NRG brand-forward page.
- Full-width green banner: `var(--nrg-green)` background, white "WHY NORTHERN NRG" heading in DM Sans Bold and Bebas Neue sub-label "OUR COMMITMENT TO QUALITY"
- Stats grid below (3-4 stat boxes): e.g. "500+ Installations", "25-Year Warranty", "Alberta-Based", "10-Year Service"
- Quote/testimonial callout block in `var(--nrg-light-green-bg)` with a bordered left stripe in `var(--nrg-green)`
- Body paragraph about NRG's Alberta expertise, local support, and long-term commitment
- Use DM Sans Bold for stat numbers, DM Sans Regular for labels

**Create src/app/proposal/pages/WarrantyPage.tsx (Page 9)**
Layout: 4 warranty cards in a 2×2 grid on `var(--nrg-page-bg)` background.
- Page heading: "WARRANTY & PROTECTION" (DM Sans Bold) with Bebas Neue sub-label "YOUR INVESTMENT IS PROTECTED"
- 4 cards in white (`var(--nrg-card-bg)`) with `var(--nrg-green)` top border accent:
  1. **Panel Warranty** — 25-year product + 25-year performance guarantee, 80% output guaranteed
  2. **Inverter Warranty** — 12-year manufacturer warranty, extended options available
  3. **Workmanship Warranty** — 10-year installation warranty by Northern NRG
  4. **Roof Penetration Warranty** — 10-year waterproofing warranty on all roof penetrations
- Each card: simple SVG icon (sun, lightning, tools, roof shape), warranty title in DM Sans Bold, term in large green text, brief description

**Create src/app/proposal/pages/FAQPage.tsx (Page 10)**
Layout: FAQ panels on `var(--nrg-page-bg)`.
- Heading: "FREQUENTLY ASKED QUESTIONS" (DM Sans Bold) with Bebas Neue sub-label
- 4-6 FAQ items as white card panels with `var(--nrg-green)` left accent border. Each panel: bold question, paragraph answer.
- Questions (hardcoded):
  1. "How does net metering work in Alberta?" — explanation of surplus credits
  2. "What happens on cloudy or winter days?" — grid connection explanation
  3. "How long does installation take?" — 2-5 day typical timeline
  4. "Will solar affect my home insurance?" — typically covered, notify insurer
  5. "What maintenance does solar require?" — annual cleaning, minimal
  6. "How is system size determined?" — based on consumption, roof, budget

**Create src/app/proposal/pages/NextStepsPage.tsx (Page 11)**
Layout: 6-step vertical timeline on `var(--nrg-page-bg)`.
- Heading: "YOUR NEXT STEPS" (DM Sans Bold) with Bebas Neue sub-label "FROM PROPOSAL TO POWER"
- Vertical timeline with 6 steps. Each step:
  - Step number: large circle in `var(--nrg-green)`, white number in Bebas Neue
  - Timeline connector line: `var(--nrg-green)` 2px vertical line between steps
  - Heading: DM Sans Bold in `var(--nrg-text-heading)`
  - Week indicator: small badge in `var(--nrg-light-green-bg)` with `var(--nrg-green)` text (e.g. "Week 1-2")
  - Description: DM Sans Regular in `var(--nrg-text-secondary)`
Steps:
  1. Sign Agreement — Review and sign your solar installation agreement (Week 1)
  2. Site Survey — Our team measures your roof and confirms system design (Week 1-2)
  3. Permitting — We handle all municipal permits and utility applications (Week 2-4)
  4. Equipment Order — Panels, inverter, and mounting hardware ordered (Week 3-4)
  5. Installation — Typically 2-5 days for full system installation (Week 5-7)
  6. Grid Connection — Utility inspection and permission to operate (Week 6-8)
- Call-to-action at bottom: "Ready to Get Started?" with NRG contact prompt in `var(--nrg-green)` box

**Final update to src/app/proposal/page.tsx:**
Import all remaining pages. Replace all placeholder divs with the actual page components. The complete render order must be:
1. CoverPage (Page 1)
2. TableOfContentsPage (Page 2) — from plan 03 Task 1
3. NetMeteringPage (Page 3) — from plan 02
4. CarbonCreditsPage (Page 4) — from plan 02
5. AllInCostsPage (Page 5) — from plan 02
6. PriceHistoryPage (Page 6) — from plan 03 Task 1
7. WhatsComingPage (Page 7)
8. WhyUsPage (Page 8)
9. WarrantyPage (Page 9)
10. FAQPage (Page 10)
11. NextStepsPage (Page 11)

Note: Plans 02 and 03 both modify page.tsx. Since they run in parallel (Wave 2), the executor for this plan should import and slot in pages 2, 6, 7, 8, 9, 10, 11 while leaving slots 1, 3, 4, 5 for plan 02's components. If plan 02 ran first (or concurrently), merge both sets of imports without overwriting each other's work.
  </action>
  <verify>
    <automated>cd "D:/Cursor/Solar Presenter" && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>All 5 static pages created. page.tsx imports and renders all 11 page components in order. TypeScript compiles with no errors. Running npm run dev and scrolling through /proposal shows all 11 pages with content.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes
2. `npm run dev` — /proposal renders 11 scrollable pages with scroll-snap behavior
3. Page 2 (TOC): 9 numbered sections with red circular badges visible
4. Page 6 (Price History): upward-trending SVG chart rendered (not blank)
5. Pages 7-11: content-rich, not blank — headings, cards, and body text visible
6. All pages use NRG green/dark green/gold color palette — no default blue links or unstyled elements
7. Scrolling through all 11 pages snaps cleanly between pages (CSS scroll-snap working)
</verification>

<success_criteria>
- All 7 static pages (2, 6-11) render with complete content and NRG branding
- page.tsx renders exactly 11 proposal-page sections in the scroll-snap container
- No TypeScript errors
- Scrolling feels like swiping through presentation slides
</success_criteria>

<output>
After completion, create `.planning/phases/04-proposal-rendering/04-03-SUMMARY.md`
</output>

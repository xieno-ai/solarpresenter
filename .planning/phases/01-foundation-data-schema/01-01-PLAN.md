---
phase: 01-foundation-data-schema
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.ts
  - tailwind.config.ts
  - postcss.config.mjs
  - .env.local.example
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/app/globals.css
  - src/lib/types/proposal-inputs.ts
  - src/lib/types/proposal-outputs.ts
  - src/lib/types/alberta-config.ts
  - src/lib/types/index.ts
  - src/lib/decimal/index.ts
  - src/lib/engine/calculate.ts
  - src/test-data/paul-friesen.ts
autonomous: true
requirements:
  - CALC-09
must_haves:
  truths:
    - "Next.js dev server starts and renders a page at localhost:3000"
    - "ProposalInputs and ProposalOutputs types are importable from src/lib/types"
    - "All monetary fields in ProposalInputs and ProposalOutputs use Decimal type, never native Number"
    - "Decimal.js is configured with precision 20 and ROUND_HALF_UP rounding"
    - "Paul Friesen test fixture constructs valid ProposalInputs using Decimal values"
  artifacts:
    - path: "src/lib/types/proposal-inputs.ts"
      provides: "ProposalInputs type with nested groups (CustomerInfo, SystemInfo, ConsumptionInfo, RateInfo, FinancingInfo)"
      exports: ["ProposalInputs", "CustomerInfo", "SystemInfo", "ConsumptionInfo", "RateInfo", "FinancingInfo"]
    - path: "src/lib/types/proposal-outputs.ts"
      provides: "ProposalOutputs type with calculation result shapes"
      exports: ["ProposalOutputs", "MonthlyNetMetering", "CarbonCreditProjection", "SavingsSummary"]
    - path: "src/lib/decimal/index.ts"
      provides: "Decimal.js configuration and helper utilities"
      exports: ["d", "toFixed", "toNumber", "toJSON", "fromJSON", "Decimal"]
    - path: "src/test-data/paul-friesen.ts"
      provides: "Reference test case for Phase 3 validation"
      exports: ["paulFriesenInputs"]
  key_links:
    - from: "src/test-data/paul-friesen.ts"
      to: "src/lib/types/proposal-inputs.ts"
      via: "imports ProposalInputs type"
      pattern: "import.*ProposalInputs.*from"
    - from: "src/test-data/paul-friesen.ts"
      to: "src/lib/decimal/index.ts"
      via: "uses d() helper for all monetary values"
      pattern: "import.*\\{.*d.*\\}.*from"
    - from: "src/lib/engine/calculate.ts"
      to: "src/lib/types/index.ts"
      via: "imports both ProposalInputs and ProposalOutputs"
      pattern: "import.*Proposal(Inputs|Outputs)"
---

<objective>
Scaffold the Next.js project into the existing repository (preserving Playwright and scripts), install all Phase 1 dependencies, create TypeScript data contracts for the entire proposal pipeline, configure Decimal.js for financial arithmetic, and establish the Paul Friesen reference test fixture.

Purpose: Every subsequent phase builds against these types and utilities. Getting the data contracts and precision arithmetic right here prevents rework in Phases 2-9.
Output: A running Next.js dev server with importable types, Decimal.js utilities, a stub calculation engine, and a hardcoded test fixture.
</objective>

<execution_context>
@C:/Users/David/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/David/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-data-schema/01-CONTEXT.md
@.planning/phases/01-foundation-data-schema/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Next.js project with all dependencies</name>
  <files>package.json, tsconfig.json, next.config.ts, tailwind.config.ts, postcss.config.mjs, .env.local.example, src/app/layout.tsx, src/app/page.tsx, src/app/globals.css</files>
  <action>
Initialize Next.js into the existing project. The project already has a `package.json` with Playwright (^1.58.2) and a `scripts/` directory with `extract-sunpitch.mjs`, plus a `reference/` directory with 11 page PNGs. These MUST be preserved.

**Do NOT use `create-next-app`** — it would overwrite `package.json` and potentially delete existing directories. Instead, manually install and configure:

1. Install Next.js 16 and all dependencies into the existing package.json:
   ```bash
   npm install next@latest react@latest react-dom@latest typescript @types/react @types/node decimal.js @supabase/supabase-js @supabase/ssr
   npm install -D @tailwindcss/postcss tailwindcss
   ```

2. Create `tsconfig.json` with App Router path aliases:
   - `compilerOptions.paths`: `"@/*": ["./src/*"]`
   - `target`: `ES2017`, `lib`: `["dom", "dom.iterable", "esnext"]`
   - `strict`: true, `module`: `esnext`, `moduleResolution`: `bundler`
   - `jsx`: `preserve`, `incremental`: true, `esModuleInterop`: true
   - `resolveJsonModule`: true, `isolatedModules`: true
   - `include`: `["next-env.d.ts", "**/*.ts", "**/*.tsx"]`, `exclude`: `["node_modules"]`

3. Create `next.config.ts`:
   ```typescript
   import type { NextConfig } from 'next';
   const nextConfig: NextConfig = {};
   export default nextConfig;
   ```

4. Create `postcss.config.mjs`:
   ```javascript
   const config = {
     plugins: {
       "@tailwindcss/postcss": {},
     },
   };
   export default config;
   ```

5. Create `src/app/globals.css` with Tailwind v4 import:
   ```css
   @import "tailwindcss";
   ```

6. Create `src/app/layout.tsx` — root layout with html, body, metadata (title: "Solar Presenter"), importing globals.css. Use a distinctive font pairing per the global CLAUDE.md guidelines — pick something elegant, NOT Inter/Roboto/Arial. Import from `next/font/google`.

7. Create `src/app/page.tsx` — placeholder home page that displays "Solar Presenter" heading and a status message indicating Phase 1 scaffold is running. Keep it minimal but functional to prove the dev server works.

8. Create `.env.local.example` with placeholder values:
   ```
   NEXT_PUBLIC_SUPABASE_URL=your-supabase-url
   NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key
   ```

9. Update `package.json` scripts:
   ```json
   "scripts": {
     "dev": "next dev",
     "build": "next build",
     "start": "next start"
   }
   ```
   Keep existing "test" script. Preserve all existing dependencies (Playwright).

10. Create `tailwind.config.ts` — can be minimal since Tailwind v4 auto-detects content. Only needed if custom theme extensions are required.

**Critical:** After setup, verify `npm run dev` starts without errors and the page renders at localhost:3000.
  </action>
  <verify>
    <automated>cd "D:/Cursor/Solar Presenter" && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Next.js dev server starts, page renders at localhost:3000, existing Playwright dependency and scripts/ directory preserved in package.json</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create TypeScript data contracts, Decimal.js utilities, and test fixture</name>
  <files>src/lib/types/proposal-inputs.ts, src/lib/types/proposal-outputs.ts, src/lib/types/alberta-config.ts, src/lib/types/index.ts, src/lib/decimal/index.ts, src/lib/engine/calculate.ts, src/test-data/paul-friesen.ts</files>
  <behavior>
    - Decimal.js configured with precision 20, ROUND_HALF_UP rounding at module load
    - d('0.1').plus('0.2').toString() === '0.3' (precision test)
    - toFixed(d('1234.5'), 2) === '1234.50'
    - toJSON(d('123.456')) === '123.456' (string serialization)
    - fromJSON('123.456') instanceof Decimal === true
    - paulFriesenInputs satisfies ProposalInputs type (TypeScript compilation)
    - paulFriesenInputs.system.monthlyProductionKwh has exactly 12 elements
    - paulFriesenInputs.rates.allInRate is a Decimal instance
    - All monetary/energy fields in ProposalInputs are Decimal type (not number)
    - latitude and longitude in CustomerInfo are number (not Decimal — non-financial)
    - financeTermMonths is number (integer count, not financial)
  </behavior>
  <action>
1. Create `src/lib/decimal/index.ts`:
   - Configure Decimal.set({ precision: 20, rounding: Decimal.ROUND_HALF_UP })
   - Export helper `d(value: string | number | Decimal): Decimal`
   - Export `toFixed(value: Decimal, places?: number): string` (default 2 places)
   - Export `toNumber(value: Decimal): number` (for non-financial use like charts)
   - Export `toJSON(value: Decimal): string` and `fromJSON(value: string): Decimal`
   - Re-export `Decimal` class

2. Create `src/lib/types/proposal-inputs.ts` with nested grouping from RESEARCH.md:
   - `CustomerInfo`: name (string), address (string), latitude (number), longitude (number)
   - `SystemInfo`: systemSizeKw (Decimal), annualProductionKwh (Decimal), monthlyProductionKwh (12-element tuple of Decimal)
   - `ConsumptionInfo`: annualConsumptionKwh (Decimal), monthlyConsumptionKwh (12-element tuple of Decimal), annualElectricityCost (Decimal)
   - `RateInfo`: allInRate (Decimal), netMeteringBuyRate (Decimal), netMeteringSellRate (Decimal), annualEscalationRate (Decimal)
   - `FinancingInfo`: cashPurchasePrice (Decimal), financeMonthlyPayment (Decimal), financeTermMonths (number — integer, not Decimal), financeInterestRate (Decimal)
   - `ProposalInputs`: { customer, system, consumption, rates, financing }

   For the 12-element tuple type, define a helper type:
   ```typescript
   export type MonthlyValues = [Decimal, Decimal, Decimal, Decimal, Decimal, Decimal, Decimal, Decimal, Decimal, Decimal, Decimal, Decimal];
   ```

3. Create `src/lib/types/proposal-outputs.ts` — stub shapes for Phase 3:
   - `MonthlyNetMetering`: month, consumptionKwh, productionKwh, gridBuyKwh, surplusSoldKwh, costToBuy, revenueEarned (all Decimal except month:string)
   - `CarbonCreditProjection`: annualCo2Avoided, tenYearPayoutLow, tenYearPayoutHigh (Decimal), benchmarkSchedule array
   - `SavingsSummary`: utilityAvoided, netMeteringRevenue, carbonCredits, cashBack, totalSavings, netSavingsAfterCost (all Decimal)
   - `ProposalOutputs`: solarOffsetPercent, twentyYearUtilityCost, twentyYearSavings, monthlyNetMetering[], carbonCredits, cashPurchase (20yr + 30yr SavingsSummary), financeOption, utilityProjection20Year, utilityProjection30Year

4. Create `src/lib/types/alberta-config.ts`:
   - `CarbonBenchmarkEntry`: year (number), pricePerTonne (Decimal), payoutLow (Decimal), payoutHigh (Decimal)
   - `AlbertaConfig`: id (string), effectiveFrom (string — ISO date), effectiveTo (string | null), gridEmissionFactor (Decimal), payoutPercentageLow (Decimal), payoutPercentageHigh (Decimal), cashBackRate (Decimal), defaultEscalationRate (Decimal), defaultNetMeteringBuyRate (Decimal), defaultNetMeteringSellRate (Decimal), carbonBenchmarkSchedule (CarbonBenchmarkEntry[])

5. Create `src/lib/types/index.ts` — barrel export all types from the above three files.

6. Create `src/lib/engine/calculate.ts` — stub function:
   ```typescript
   export function calculateProposal(inputs: ProposalInputs): ProposalOutputs {
     // Phase 3: Replace with real calculation engine
     throw new Error('Calculation engine not yet implemented (Phase 3)');
   }
   ```

7. Create `src/test-data/paul-friesen.ts`:
   - Import `d` from `@/lib/decimal`
   - Import `ProposalInputs` from `@/lib/types`
   - Export `paulFriesenInputs: ProposalInputs` with Paul Friesen data:
     - Customer: "Paul Friesen", address placeholder, Edmonton lat/long
     - System: 12.24 kW, 15408 kWh annual, 12 monthly production values (use plausible Alberta solar curve values — exact values will be updated from calculations document in Phase 3)
     - Consumption: 12000 kWh annual, 12 monthly values, $4,212 annual cost
     - Rates: all-in 0.3510, buy 0.1680, sell 0.3350, escalation 0.05
     - Financing: $30,000 cash, $189/mo finance, 240 months, 6.99%
   - ALL monetary/energy values constructed via `d('string')` — never from float literals.

8. Write a simple test file `src/lib/decimal/decimal.test.ts` using basic Node assert (no test framework needed yet):
   - Verify precision: `d('0.1').plus('0.2').toString() === '0.3'`
   - Verify rounding: `d('1.005').toFixed(2) === '1.01'` (ROUND_HALF_UP)
   - Verify serialization roundtrip: `fromJSON(toJSON(d('123.456'))).eq(d('123.456'))`

**Critical anti-pattern:** Never write `d(0.1)` — always `d('0.1')`. Constructing from float bakes in IEEE 754 error.
  </action>
  <verify>
    <automated>cd "D:/Cursor/Solar Presenter" && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <done>All TypeScript types compile without errors. ProposalInputs and ProposalOutputs are importable. Paul Friesen fixture constructs valid typed data. Decimal.js is configured and helper functions work. All monetary fields use Decimal, never native Number.</done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds with no TypeScript errors
2. `npx tsc --noEmit` passes — all types are valid
3. Existing `scripts/extract-sunpitch.mjs` and `reference/` directory still present
4. `package.json` still contains `playwright` dependency
5. Paul Friesen test data compiles against ProposalInputs type
6. Decimal.js precision test: `d('0.1').plus('0.2').toString() === '0.3'`
</verification>

<success_criteria>
- Next.js 16 application builds and runs locally
- ProposalInputs and ProposalOutputs TypeScript types defined with nested grouping and Decimal fields
- Decimal.js configured with precision 20, ROUND_HALF_UP, helper utilities exported
- Paul Friesen reference fixture compiles and uses Decimal for all financial values
- AlbertaConfig type defined matching the Supabase schema design
- Stub calculation engine accepts ProposalInputs and returns ProposalOutputs (throws for now)
- Existing project files (Playwright, scripts, reference images) preserved
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-data-schema/01-01-SUMMARY.md`
</output>
